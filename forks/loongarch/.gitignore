# Object files
*.ko
*.obj
*.elf

# Linker output
*.ilk
*.map
*.exp

# Precompiled Headers
*.gch
*.pch

# Libraries
*.lib
*.a
*.la
*.lo

# Shared objects (inc. Windows DLLs)
*.dll
*.so
*.so.*
*.dylib

# Executables
*.exe
*.out
*.app
*.i*86
*.x86_64
*.hex

# Debug files
*.dSYM/
*.su
*.idb
*.pdb

# Kernel Module Compile Results
*.mod*
*.cmd
.tmp_versions/
modules.order
Module.symvers
Mkfile.old
dkms.conf

.vscode/*
/kernel/kernel
fs.img
.gdb_history
# claude: anchored - the host tool lives in tools/ since Phase 0
/tools/mkfs
initcode
/ulib/usys.S
# claude: was tracked (a generated "xxd -i fs.img" C header, up to several
# MB, that changes on every build - see notes_arch_loongarch.txt), same
# oversight as mkfs/initcode/usys.S above evidently were before this
# .gitignore already listed them - the port's own fs.img is embedded
# straight into the kernel image via this file rather than a separate
# disk image, but it's still purely a build product, not source.
kernel/ramdisk.h